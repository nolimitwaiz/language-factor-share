# One-command regeneration of paper tables/figures from results/ (Phase 4.2).
# Stub until Phase 4; each phase adds its targets here.

.PHONY: reproduce-tables test lint

reproduce-tables:
	@echo "reproduce-tables: stub — populated in Phase 4 (pinned run IDs from results/runs/)"
	@exit 1

test:
	pytest

lint:
	ruff check src tests scripts
