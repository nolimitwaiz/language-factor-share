# Analysis (CPU): everything that reads stored results and reproduces tables and figures
numpy>=1.26
scipy>=1.11
pandas>=2.1
pyarrow>=14
scikit-learn>=1.4
statsmodels>=0.14
pyyaml>=6
matplotlib>=3.8
pillow>=10
pypdf>=4
python-pptx>=0.6.23
pytest>=8
# Model-touching steps (cluster only): hidden-state extraction, benchmarks, training
# torch>=2.3
# transformers>=4.46
# lm-eval  (EleutherAI lm-evaluation-harness, for Belebele and INCLUDE)
